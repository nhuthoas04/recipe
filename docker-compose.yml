version: '3.8'

services:
  # Backend API (Express.js)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.backend
    container_name: recipe-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MONGODB_URI=mongodb+srv://nhuthoas04:123@cluster0.awyu0je.mongodb.net/goiymonan?retryWrites=true&w=majority&appName=Cluster0
      - JWT_SECRET=8f9e7d6c5b4a3928171615141312111009080706050403020100abcdefghijklmnop
      - FRONTEND_URL=http://localhost:3000
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_SECURE=false
      - SMTP_USER=nhuthoas04@gmail.com
      - SMTP_PASS=xxcoulnsqilnyeon
      - SMTP_FROM=nhuthoas04@gmail.com
    networks:
      - recipe-network

  # Frontend (Next.js)
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: recipe-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb+srv://nhuthoas04:123@cluster0.awyu0je.mongodb.net/goiymonan?retryWrites=true&w=majority&appName=Cluster0
      - JWT_SECRET=8f9e7d6c5b4a3928171615141312111009080706050403020100abcdefghijklmnop
      - NEXT_PUBLIC_API_URL=http://localhost:5000
      - INTERNAL_API_URL=http://backend:5000
    depends_on:
      - backend
    networks:
      - recipe-network

networks:
  recipe-network:
    driver: bridge
